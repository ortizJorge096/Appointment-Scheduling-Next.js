# Secret SOLO para local (minikube/docker-desktop con Postgres en compose).
# Reemplaza el stub del base via merge.
apiVersion: v1
kind: Secret
metadata:
  name: appointment-scheduling-secret
type: Opaque
stringData:
  # Si corres Postgres en el host (port 5432 expuesto), usa el host.docker.internal
  # del nodo k8s. Ajusta según tu setup.
  DATABASE_URL: "postgresql://valentinajimenez:valentinajimenez123@host.docker.internal:5432/valentinajimenez?schema=public"
  NEXTAUTH_SECRET: "local-dev-secret-change-me"
