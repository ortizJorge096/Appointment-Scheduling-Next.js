# Empty stub — real values are injected by:
#   - in production: the ec2-k3s module user-data, which reads them from
#     SSM Parameter Store and creates/updates the Secret before apply.
#   - locally: the `local` overlay provides its own values for minikube.
#
# The name must match the Deployment's envFrom.
apiVersion: v1
kind: Secret
metadata:
  name: appointment-scheduling-secret
type: Opaque
stringData:
  # DATABASE_URL: "postgresql://user:pass@host:5432/db?schema=public&sslmode=require"
  # NEXTAUTH_SECRET: "stub"
  PLACEHOLDER: "replaced-at-deploy-time"
